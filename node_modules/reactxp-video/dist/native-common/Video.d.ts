/// <reference types="react" />
import RX = require('reactxp');
import Types = require('../common/Types');
export interface VideoState {
    isPlaying?: boolean;
    isMuted?: boolean;
    duration?: number;
}
declare class Video extends RX.Component<Types.VideoProps, VideoState> {
    private _mountedComponent;
    constructor(props: Types.VideoProps);
    render(): JSX.Element;
    private _onMount;
    private _onError;
    private _onLoad;
    private _onLoadData;
    private _onBuffer;
    private _onEnd;
    seek(position: number): void;
    seekPercent(percentage: number): void;
    play(): void;
    pause(): void;
    stop(): void;
    mute(muted: boolean): void;
}
export default Video;
